<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Primary Application Theme (Material 3 Master) -->
    <style name="Theme.ChessApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Core Color Mapping -->
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/brand_on_primary</item>
        <item name="colorPrimaryContainer">@color/brand_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/brand_on_primary_container</item>

        <item name="colorSecondary">#4D7678</item>
        <item name="colorOnSecondary">#FFFFFF</item>

        <item name="android:windowBackground">@color/brand_background</item>
        <item name="colorSurface">@color/brand_surface</item>
        <item name="colorOnSurface">@color/colorTextPrimaryOnSurface</item>
        <item name="colorSurfaceVariant">@color/brand_surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/colorTextSecondaryOnSurface</item>

        <item name="colorOutline">@color/brand_outline</item>
        <item name="android:statusBarColor">@color/brand_background</item>
        <item name="android:navigationBarColor">@color/brand_background</item>

        <!-- Custom Semantic Text Attributes -->
        <item name="android:textColorPrimary">@color/colorTextPrimaryOnSurface</item>
        <item name="android:textColorSecondary">@color/colorTextSecondaryOnSurface</item>

        <!-- Component Branding -->
        <item name="materialButtonStyle">@style/Widget.ChessApp.Button.Main</item>
        <item name="materialCardViewStyle">@style/Widget.ChessApp.Card.Glass</item>
        <item name="textInputStyle">@style/Widget.ChessApp.Input.Outlined</item>
    </style>

    <!-- Elevated Buttons -->
    <style name="Widget.ChessApp.Button.Main" parent="Widget.Material3.Button">
        <item name="shapeAppearance">@style/ShapeAppearance.ChessApp.Button</item>
        <item name="android:minHeight">@dimen/brand_button_height</item>
        <item name="android:paddingHorizontal">@dimen/brand_spacing_l</item>
        <item name="android:letterSpacing">0.1</item>
        <item name="android:fontFamily">sans-serif-black</item>
        <item name="android:textSize">@dimen/brand_text_label</item>
        <item name="android:textColor">@color/brand_on_primary</item>
    </style>

    <style name="Widget.ChessApp.Button.Tonal" parent="Widget.Material3.Button.TonalButton">
        <item name="shapeAppearance">@style/ShapeAppearance.ChessApp.Button</item>
        <item name="android:minHeight">@dimen/brand_button_height</item>
    </style>

    <!-- Glassmorphic Cards -->
    <style name="Widget.ChessApp.Card.Glass" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">@dimen/brand_corner_l</item>
        <item name="cardElevation">@dimen/brand_card_elevation</item>
        <item name="cardBackgroundColor">@color/brand_surface</item>
    </style>

    <!-- Outlined Input Fields -->
    <style name="Widget.ChessApp.Input.Outlined" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="shapeAppearance">@style/ShapeAppearance.ChessApp.Input</item>
        <item name="boxStrokeColor">@color/brand_primary</item>
        <item name="android:textColorHint">?attr/colorOnSurfaceVariant</item>
    </style>

    <style name="Widget.ChessApp.Input.Dropdown" parent="Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="shapeAppearance">@style/ShapeAppearance.ChessApp.Input</item>
        <item name="boxStrokeColor">@color/brand_primary</item>
        <item name="android:textColorHint">?attr/colorOnSurfaceVariant</item>
        <!-- Background Blur for Menu (API 31+) -->
        <item name="materialThemeOverlay">@style/ThemeOverlay.ChessApp.ExposedDropdownMenu</item>
    </style>

    <style name="ThemeOverlay.ChessApp.ExposedDropdownMenu" parent="">
        <item name="listPopupWindowStyle">@style/Widget.ChessApp.ListPopupWindow</item>
    </style>

    <style name="Widget.ChessApp.ListPopupWindow" parent="Widget.Material3.PopupMenu">
        <item name="android:popupBackground">@drawable/bg_dropdown_menu</item>
        <!-- Native window blur available on Android 12+ -->
        <item name="android:windowBlurBehindEnabled" tools:targetApi="s">true</item>
        <item name="android:windowBlurBehindRadius" tools:targetApi="s">20dp</item>
    </style>

    <!-- Shape Tokens -->
    <style name="ShapeAppearance.ChessApp.Button" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerSize">0dp</item>
    </style>

    <style name="ShapeAppearance.ChessApp.Input" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerSize">@dimen/brand_corner_s</item>
    </style>

    <!-- Typography Styles using Semantic Colors -->
    <style name="TextAppearance.ChessApp.Headline" parent="TextAppearance.Material3.HeadlineMedium">
        <item name="android:fontFamily">sans-serif-black</item>
        <item name="android:textSize">@dimen/brand_text_h3</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
    </style>

    <style name="TextAppearance.ChessApp.Label" parent="TextAppearance.Material3.LabelLarge">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">@dimen/brand_text_label</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:letterSpacing">0.1</item>
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
    </style>

</resources>